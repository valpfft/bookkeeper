<%= form.error_notification %>
<%= form.input :name, autofocus: true, placeholder: "Name" %>
<%= form.input :amount %>
<%= form.association :category, collection: Category.all.where(user: current_user).order(:name), prompt: "Choose a category" %>
